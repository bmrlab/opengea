# Server-only configuration; no NEXT_PUBLIC variables.
APP_ORIGIN=http://localhost:3000
SESSION_SECRET=
GEA_MODE=local
GEA_AGENT_URL=http://127.0.0.1:8787/gea/agents/tech-news
# Hosted mode only. Copy the matching Agent base URL WITHOUT /run.
# The Project key needs runs:write and chats:read.
GEA_PROJECT_API_KEY=
