# Used only by the local Agent process. Never copied into apps/web or the Agent package.
CREATIVE_REASONING_API_KEY=
# Ask before each search by default. Set to false to disable approval.
REQUIRE_SEARCH_APPROVAL=true
