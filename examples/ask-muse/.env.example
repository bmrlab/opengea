# Public client ID; register each callback URL you intend to use with MuseDAM.
MUSEDAM_MCP_CLIENT_ID=muse-mcp
# Needed only for local model calls, not hosted Studio execution.
CREATIVE_REASONING_API_KEY=
