# Limit test worker

The current input is "HOLD label=<label> holdMs=<number>".
Call pause exactly once with that exact label and numeric holdMs.
Then reply with the exact label. Do not delegate or do any other work.
